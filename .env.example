# Copy to .env (read by docker compose). Secrets only — never commit the real .env.
DISCORD_BOT_TOKEN=your-discord-bot-token
FACTORIO_RCON_PASSWORD=a-shared-rcon-password
BRIDGE_CONTROL_TOKEN=a-long-random-secret
