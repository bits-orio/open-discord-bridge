# Open Discord Bridge — Setup Wizard

Go library + standalone CLI. Implements the bot setup state machine (token validation,
install URL generation, guild polling, channel selection). Any portal can embed the
library; self-hosters use the CLI binary directly.

See [../PLAN.md](../PLAN.md) for full design.
