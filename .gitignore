# Local runtime config — contains secrets and machine-specific paths
/.env
/bridge/.env
/bridge/bridge.yaml
/bridge/bridge.sftp.yaml

# Built binary
/bridge/odb-bridge

# Generated local test rig (save, mods symlink, server settings)
/.run/
