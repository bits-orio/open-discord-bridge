# Local runtime config — contains secrets and machine-specific paths
/.env
/bridge/.env
/bridge/bridge.yaml
/bridge/bridge-local.yaml
/bridge/bridge.sftp.yaml

# Built binaries
/bridge/odb-bridge
/wizard/odb-wizard

# Runtime state written by the bridge (contains Discord IDs)
/bridge/links.json
/bridge/links.json.tmp

# Generated local test rig (save, mods symlink, server settings)
/.run/

# Git worktrees for feature branches
/.worktrees/
